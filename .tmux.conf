# prefix to a
unbind-key C-b
set -g prefix C-a

# don't rename windows automatically
set-option -g allow-rename off

set-option -g default-shell $SHELL
set -g default-command "reattach-to-user-namespace -l ${SHELL}"

# # Allow xterm titles in terminal window, terminal scrolling with scrollbar, and setting overrides of C-Up, C-Down, C-Left, C-Right
# set -g terminal-overrides "xterm*:XT:smcup@:rmcup@:kUP5=\eOA:kDN5=\eOB:kLFT5=\eOD:kRIT5=\eOC"

# # Set ability to capture on start and restore on exit window data when running an application
setw -g alternate-screen on

# act like vim
setw -g mode-keys vi
set -g status-keys vi
# bind-key -T vi-edit Up history-up
# bind-key -T vi-edit Down history-down

# copy-mode
bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "reattach-to-user-namespace pbcopy"
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "reattach-to-user-namespace pbcopy"

# turn on mouse support, note plugin below
set -g mouse on

# nicer splitting keys
unbind-key %
# open splits with the current path
bind-key | split-window -h -c '#{pane_current_path}'
bind-key \ split-window -h -c '#{pane_current_path}'
bind-key - split-window -v -c '#{pane_current_path}'

# create a small vertical pane at the bottom
unbind-key m
bind-key m split-window -v -l 8
bind-key C-x kill-pane

# resize pane, -r is may repeat
bind-key -r J resize-pane -D 5
bind-key -r K resize-pane -U 5
bind-key -r H resize-pane -L 5
bind-key -r L resize-pane -R 5

# s to toggle sync
unbind-key s
bind-key s set-window-option synchronize-panes

# Status bar.
# set -g status-bg '#3c3836'
# set -g status-fg white
# set -g status-left-length 40
# set -g status-left '#[fg=yellow]#S § ' # SECTION SIGN (UTF-8: c2 a7)
# set -g status-right "#[fg=yellow]$USER@#h"
# set -g status-interval 60 # Default is 15.

# # Easy config reload
bind-key R source-file ~/.tmux.conf \; display-message "tmux.conf reloaded."

# Highlight active window.
set -w -g window-status-current-bg red

# Scroll 3 lines at a time instead of default 5; don't extend dragged selections.
bind-key -T copy-mode-vi WheelUpPane select-pane\; send-keys -t{mouse} -X clear-selection\; send-keys -t{mouse} -X -N 3 scroll-up
bind-key -T copy-mode-vi WheelDownPane select-pane\; send-keys -t{mouse} -X clear-selection\; send-keys -t{mouse} -X -N 3 scroll-down

# Make double and triple click work outside of copy mode (already works inside it with default bindings).
bind-key -T root DoubleClick1Pane if-shell -Ft{mouse} '#{alternate_on}' "send-keys -M" "copy-mode -t{mouse}; send-keys -t{mouse} -X select-word"
bind-key -T root TripleClick1Pane if-shell -Ft{mouse} '#{alternate_on}' "send-keys -M" "copy-mode -t{mouse}; send-keys -t{mouse} -X select-line"

#start window and pane numbering at 1
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1

# Needed as on tmux 1.9 and up (defaults to off).
# Added in tmux commit c7a121cfc0137c907b7bfb.
set -g focus-events on

# But don't change tmux's own window titles.
set -w -g automatic-rename off

# #T      = standard window title (last command, see ~/.bash_profile)
# #h      = short hostname
# #S      = session name
# #W      = tmux window name
#
# (Would love to include #(pwd) here as well, but that would only print the
# current working directory relative to the session -- ie. always the starting
# cwd -- which is not very interesting).
set -g set-titles-string "#T : #h > #S > #W"

# Turn off distracting border highlight: the background highlighting is probably
# enough. Default value of 8 comes from "pane-border-fg" in tmux's
# options-table.c.
set -g pane-active-border-fg 8

set -g @tpm_plugins '                \
    tmux-plugins/tpm                 \
    tmux-plugins/tmux-sensible       \
    tmux-plugins/tmux-resurrect      \
    tmux-plugins/tmux-continuum      \
    tmux-plugins/tmux-yank           \
    tmux-plugins/tmux-pain-control   \
    tmux-plugins/tmux-copycat        \
    tmux-plugins/tmux-open           \
    nhdaly/tmux-better-mouse-mode    \
'

# Initialize TMUX plugin manager
run '~/.tmux/plugins/tpm/tpm'

# truecolor
# set-option -ga terminal-overrides ",xterm-256color:Tc"
# set -g default-terminal "screen-256color-italic"
# set -g default-terminal "screen-256color"
# set -as terminal-overrides ',xterm*:sitm=\E[3m'
set -g default-terminal "tmux-256color"
# set -ga terminal-overrides ',*:Ss=\E[%p1%d q:Se=\E[2 q'
# set -ga terminal-overrides ',*:Ss=\E[%p1%d q:Se=\E[2 q,tmux-256color:tc'
set -ga terminal-overrides ",tmux-256color:Tc"
# set -g default-terminal "xterm-256color"
# set -as terminal-overrides ',xterm*:sitm=\E[3m'
#
# #504945
# #665c54
# #a89984
set -g status-interval 60 # Default is 15.

# statusline from vim plugin
set -g status-justify "left"
set -g status "on"
set -g status-attr "none"
set -g message-command-bg "#504945"
set -g status-left-length "100"
set -g pane-active-border-fg "yellow"
set -g status-bg "#3c3836"
set -g message-command-fg "#a89984"
set -g pane-border-fg "#665c54"
set -g message-bg "#504945"
set -g status-left-attr "none"
set -g status-right-attr "none"
set -g status-right-length "100"
set -g message-fg "#fbf1c7"
setw -g window-status-fg "colour246"
setw -g window-status-attr "none"
setw -g window-status-activity-bg "#3c3836"
setw -g window-status-activity-attr "none"
setw -g window-status-activity-fg "colour208"
setw -g window-status-separator ""
setw -g window-status-bg "#3c3836"
set -g status-left "#[fg=colour234,bg=yellow] #S #[fg=yellow,bg=#3c3836,nobold,nounderscore,noitalics]"
set -g status-right "#[fg=#504945,bg=#3c3836,nobold,nounderscore,noitalics]#[fg=#a89984,bg=#504945] %Y-%m-%d  %H:%M #[fg=yellow,bg=#504945,nobold,nounderscore,noitalics]#[fg=#504945,bg=yellow] $USER#h "
setw -g window-status-format "#[fg=#a89984,bg=#3c3836] #I #[fg=#a89984,bg=#3c3836] #W "
setw -g window-status-current-format "#[fg=#3c3836,bg=#a89984,nobold,nounderscore,noitalics]#[fg=colour234,bg=#a89984] #I #[fg=colour234,bg=#a89984] #W #[fg=#a89984,bg=#3c3836,nobold,nounderscore,noitalics]"

